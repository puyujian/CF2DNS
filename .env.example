# Server settings
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Cloudflare API token (required)
# Create a token in Cloudflare with the minimal permissions needed
# and paste it here before running the server.
CLOUDFLARE_API_TOKEN=

# Admin login password (required to enable login gate)
ADMIN_PASSWORD=

